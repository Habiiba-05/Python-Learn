# function

# A function is something I write once so my code won't be long,
# especially if it's something that needs to be repeated.
# A function starts with:  def fn_name():


def greeting():
    print("hello")

# Calling the function is necessary, otherwise it won't run
greeting()