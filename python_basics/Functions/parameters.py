# function parameters

# Defining a function with a parameter 'name'
def greeting(name):
    print(f'hello {name}, how are you?')

# Since the function expects a parameter, I must give a value when calling it
greeting("habiba")